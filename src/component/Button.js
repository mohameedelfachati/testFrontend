
import React from 'react';
import style from './button.module.css'

const Button = () => {
    return (
        <button className={style.buttonStyle}>Modify</button>
    );
};

export default Button;